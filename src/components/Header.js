import React from 'react'
import styles from 'components/Header.module.scss'

function Header() {
  return (
    <header className={styles.component}>
      <h2 className={styles.title}>Header will be here soon</h2>
    </header>
  )
}

export default Header
